import { createFileRoute } from "@tanstack/react-router";
import { IncidentReport } from "@/components/IncidentReport";

export const Route = createFileRoute("/")({
  head: () => ({
    meta: [
      { title: "Powiadomienie o Awarii — Incident Report" },
      { name: "description", content: "Generator powiadomień o awariach z eksportem do PNG i wysyłką e-mail." },
      { property: "og:title", content: "Powiadomienie o Awarii" },
      { property: "og:description", content: "Interaktywny formularz incydentu z eksportem PNG i mailto." },
    ],
  }),
  component: Index,
});

function Index() {
  return <IncidentReport />;
}
